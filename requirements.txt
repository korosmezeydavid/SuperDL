requests>=2.31
yt-dlp>=2025.1.1
wxpython>=4.2
feedparser>=6.0
sounddevice>=0.4
numpy>=1.24
edge-tts>=6.1
python-docx>=1.1
ebooklib>=0.18
pypdf>=4.0
# PDF-KINYERÉS: a pypdf a karakterenként pozicionált PDF-eknél
# szétszabdalja a szöveget (Turai László NAV-levele, 2026-09-21:
# „N e m z e ti A d ó - é s V á m h iv a ta l"). A pdfminer.six ezeket
# helyesen kezeli; a booktext a kettő közül a LEGÉPEBB eredményt
# választja. MIT licenc, tiszta Python, a függőségei (cryptography,
# charset-normalizer) már benne vannak a csomagban.
pdfminer.six>=20231228
fpdf2>=2.7
beautifulsoup4>=4.12
# A TITKOSÍTOTT MENTÉSHEZ (superdl/mentes.py, AES-GCM). Ez a csomag eddig
# HIÁNYZOTT innen, pedig a kód használja: a fejlesztői gépen véletlenül
# telepítve volt, ezért helyben minden zöld volt, a CI viszont MINDEN pushnál
# elbukott rajta (8 teszt, legalább 2026-08-28 óta).
# ⚠️ És ami ennél rosszabb: enélkül a mentés NEM omlik össze, hanem udvariasan
# lefokozza magát („a titkosító réteg nem érhető el ezen a gépen"). Egy tiszta
# gépen épült program tehát CSENDBEN nem tudna mentést készíteni. Ugyanaz a
# hibaosztály, mint a 4.6.1-ben a néma DHT: nem hibaüzenet, hanem hallgatás.
cryptography>=42.0
# pywin32: a beszéd (SAPI), a képernyőolvasó-vezérlés és a `store.py` DPAPI-s
# kulcstárolása (`win32crypt`) használja. Eddig CSAK a CI-ban szerepelt, kézzel
# hozzátoldva — a requirements-ben nem, pedig a program futásához kell.
# A jelölő miatt nem-Windowson nem próbálja telepíteni.
pywin32>=306; sys_platform == "win32"
# IANA időzóna-adatbázis (Windowson nincs beépítve) – az ICS TZID
# helyes értelmezéséhez KELL, különben órákkal téves emlékeztetők.
tzdata>=2024.1
# ⚠️ A LETÖLTÖTT MODULOK használják (Super Mail helyesírás-ellenőrzés,
# Távsegítség hangfelvétel). A Core sehol nem importálja, ezért ha innen
# kimarad, a build gépén nem lesz telepítve, a `collect_submodules('comtypes')`
# üresen tér vissza, és a modul a kiadott programban „fut, de halott":
# betöltődik, aztán ImportError. Pontosan így veszett el a 4.6.11 első
# buildjéből. A jelölő miatt nem-Windowson nem próbálja telepíteni.
comtypes>=1.2; sys_platform == "win32"
